module Tsp {
    export function nextInt(min: number, max: number): number {
        return Math.floor((Math.random() * max) + min);
    }

    export function quickSort(chromosomes: Array<Chromosome>, left: number, right: number) {

    }
} 